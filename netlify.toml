[build]
  command = "cd client && npm install && npm run build"
  publish = "client/dist"
  base = "/"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"
  VITE_API_URL = "REPLACE_WITH_YOUR_BACKEND_URL"
